<%= form_for(@prelogin) do |f| %>
  <% if @prelogin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prelogin.errors.count, "error") %> prohibited this prelogin from being saved:</h2>

      <ul>
      <% @prelogin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
