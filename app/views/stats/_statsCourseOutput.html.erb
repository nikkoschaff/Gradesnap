<% if @course %>
  <div class="studentResults">
    <h1>Course Results</h1>
    <% @show_hash_course[:students].each do |student| %>
      <%= link_to "#{student.full_name}", :action => 'show', :controller => 'students' %>  
      <%= "Grade: #{student.grade}" %>
    <% end %>
  </div>
<% end %>
