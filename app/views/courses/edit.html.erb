<div class="bodyWrapper">
  <div class="container">
		<h1>Editing course</h1>

		<%= render 'form' %>

		<%= link_to 'Cancel', :action => 'show', :controller => "courses", :id => @course.id %>
	</div>
</div>
