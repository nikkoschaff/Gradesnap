source 'https://rubygems.org'

#Gem is needed to interface with sqlite3.
gem 'rails'
# Gem is needed to interface with sqlite3.
gem 'sqlite3'
# Gem is needed to interface with postgresql.
gem 'ps'
# Gem is needed to interface our database to postgresql.
gem 'activerecord-postgresql-adapter'
# Gem is necessary to include captcha in our sign-up form.
gem 'ruby-recaptcha'

# To use old protected attributes
gem 'protected_attributes'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  # Gem is necessary to compile any CSS.
  gem 'sass-rails' 
  # Gem is necessary to compile any/all javascript.
  gem 'coffee-rails' 
  # ELIMINATE ELIMINATE DESTROY DESTROY
  gem 'annotate'
  # Gem is necessary to twitter implement twitter bootstrap
  gem 'bootstrap-sass'
  # Gem is necessary to twitter implement twitter bootstrap
  gem 'twitter-bootstrap-rails'
  # Gem is necessary to create JSON structures via a Builder-style.
  gem 'jbuilder'
  # Gem is necessary to used to type direct code into the html.
  gem 'google-code-prettify-rails'
  # Gem is necessary to compress javascript when compiling.
  gem 'uglifier'
  # UNKNOWN
  gem 'bson_ext'
  # It is what makes code in views work. Rails dependency gem.
  gem 'railties'
end

# Digital Ocean uses unicorn
gem 'unicorn'

# Mailer gem
gem 'mail'
# Dependency for twitter-bootstrap
gem 'less-rails'
# Gem calls javascript code and manipulate javascript objects from ruby. 
gem 'therubyracer', :platform => :ruby 
# Necessary to transfer information from nginx to browser.
gem 'passenger'
# Dependency for rails.
gem 'rack'
# Gem is what allows 'easy' error messaging.
gem 'dynamic_form'
# Gem is dependency for image processing.
gem 'gsimgproc', '0.0.2', :git => 'git@github.com:nikkoschaff/gsimgproc.git'
# Gem is necessary for image uploading and attaching.
gem 'carrierwave'
# Gem makes possible the interaction between sequel database and carrierwave.
gem 'carrierwave-sequel', :require => 'carrierwave/sequel'
# Gem is necessary for spreadsheet output.
gem 'spreadsheet'
# wrapper gem for image magick
gem 'mini_magick'
# Gem used for ajax upload.
gem 'remotipart'
# Gem supports all major cloud providers including AWS, Rackspace, Linode, Blue Box, StormOnDemand, and many others. Full support for most AWS services including EC2, S3, CloudWatch, SimpleDB, ELB, and RDS.
gem 'fog'
# Gem used to split emailed pdf's.
gem 'docsplit'
# Gem used for spreadsheet parsing.
gem 'roo'
# Dependency gem for fileuploader.
gem "jquery-fileupload-rails"
# Jquery UI - used in jquery fileupload
gem "jquery-ui-rails"
# Dependency gem for fileuploader.
gem 'jquery-rails'

# to fix aws somehow
gem 'unf'

# To use sha3 encryption
gem 'digest-sha3'
# To use ActiveModel has_secure_password
 gem 'bcrypt-ruby'
